{% extends 'base.html.twig' %}

{% block title %}TimelineStep{% endblock %}

{% block body %}
    <h1>TimelineStep</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ timeline_step.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ timeline_step.date ? timeline_step.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ timeline_step.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ timeline_step.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_timeline_step_index') }}">back to list</a>

    <a href="{{ path('app_timeline_step_edit', {'id': timeline_step.id}) }}">edit</a>

    {{ include('timeline_step/_delete_form.html.twig') }}
{% endblock %}
